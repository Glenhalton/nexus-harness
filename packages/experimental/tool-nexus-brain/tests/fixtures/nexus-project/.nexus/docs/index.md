# Fixture Brain

Test fixture used to boot `@deepseek-ai/dsh-experimental-tool-nexus-brain` for
the tool catalog generator and this package's tests — not a real project.

## ⏭️ What's Next

- Nothing — this brain exists only so `resolveBrainContext` has a `.nexus/`
  directory to resolve.
